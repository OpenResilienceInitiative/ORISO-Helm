apiVersion: v1
kind: Service
metadata:
  name: {{ include "signoz.fullname" . }}
  namespace: {{ default .Release.Namespace .Values.signoz.namespace }}
  labels:
    {{- include "signoz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.signoz.service.type }}
  ports:
  - port: {{ .Values.signoz.service.port }}
    targetPort: {{ .Values.signoz.service.targetPort }}
    protocol: TCP
    name: http
  - port: {{ .Values.signoz.service.internalPort }}
    targetPort: {{ .Values.signoz.service.internalPort }}
    protocol: TCP
    name: http-internal
  - port: {{ .Values.signoz.service.opampPort }}
    targetPort: {{ .Values.signoz.service.opampPort }}
    protocol: TCP
    name: opamp-internal
  - port: {{ .Values.signoz.service.otlpGrpcPort }}
    targetPort: {{ .Values.signoz.service.otlpGrpcPort }}
    protocol: TCP
    name: otel-grpc
  - port: {{ .Values.signoz.service.otlpHttpPort }}
    targetPort: {{ .Values.signoz.service.otlpHttpPort }}
    protocol: TCP
    name: otel-http
  selector:
    app.kubernetes.io/component: signoz
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/name: {{ include "signoz.name" . }}
  clusterIP: None  # Headless service for StatefulSet
