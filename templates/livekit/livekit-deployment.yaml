{{- $strategy := required "livekit.deploymentStrategy is required" .Values.livekit.deploymentStrategy -}}
{{- $terminationGrace := required "livekit.terminationGracePeriodSeconds is required" .Values.livekit.terminationGracePeriodSeconds -}}
{{- if not (has $strategy (list "Recreate" "RollingUpdate")) -}}
{{- fail "livekit.deploymentStrategy must be Recreate or RollingUpdate" -}}
{{- end -}}
{{- if and (eq $strategy "RollingUpdate") (lt (int .Values.livekit.replicas) 2) -}}
{{- fail "livekit.deploymentStrategy=RollingUpdate requires livekit.replicas >= 2 because hostNetwork ports cannot overlap on a single node" -}}
{{- end -}}
{{- if or (lt (int $terminationGrace) 1) (gt (int $terminationGrace) 300) -}}
{{- fail "livekit.terminationGracePeriodSeconds must be between 1 and 300 seconds" -}}
{{- end -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: livekit
  namespace: {{ .Release.Namespace }}
  labels:
    app: livekit
spec:
  replicas: {{ .Values.livekit.replicas }}
  selector:
    matchLabels:
      app: livekit
  strategy:
    type: {{ $strategy }}
    {{- if eq $strategy "RollingUpdate" }}
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 0
    {{- end }}
  template:
    metadata:
      labels:
        app: livekit
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app: livekit
              topologyKey: kubernetes.io/hostname
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      enableServiceLinks: false
      restartPolicy: Always
      terminationGracePeriodSeconds: {{ $terminationGrace }}
      containers:
        - name: livekit
          image: {{ include "oriso.immutableImage" (list "livekit.image" .Values.livekit.image) | quote }}
          imagePullPolicy: IfNotPresent
          args:
            - --config
            - /etc/livekit/config.yaml
          ports:
            - containerPort: 7880
              name: http
              protocol: TCP
          volumeMounts:
            - name: livekit-config
              mountPath: /etc/livekit
      volumes:
        - name: livekit-config
          secret:
            secretName: {{ .Values.livekit.existingConfigSecret.name | quote }}
            items:
              - key: {{ .Values.livekit.existingConfigSecret.key | quote }}
                path: config.yaml
