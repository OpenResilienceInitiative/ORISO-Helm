apiVersion: apps/v1
kind: Deployment
metadata: 
  labels:
    app: frontend
  name: frontend
spec:
  replicas: {{ .Values.frontend.replicas }}
  selector:
    matchLabels:
      app: frontend
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: frontend
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchLabels:
                  app: frontend
              topologyKey: kubernetes.io/hostname
      containers:
        - image: {{ include "oriso.immutableImage" (list "frontend.image" .Values.frontend.image) | quote }}
          name: frontend          
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: frontend-configmap
          ports:
            - containerPort: 8080
          resources: {}          
      imagePullSecrets:
        - name: registry-secret
      restartPolicy: Always
