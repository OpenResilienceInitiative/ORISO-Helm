{{- if .Values.matrixrtcLifecycle.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: matrixrtc-lifecycle-auth-headers
  namespace: {{ .Release.Namespace }}
data:
  Authorization: "$http_authorization"
  X-Original-URI: "$request_uri"
{{- end }}
