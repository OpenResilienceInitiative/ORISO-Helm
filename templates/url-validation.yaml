{{- /*
Central fail-fast gate for global.domainName (ORISO-Helm#366). About 90
template lines build public URLs, ingress hosts and mail links from it, so an
empty or placeholder value must stop `helm install` before any pod starts.
This template renders no manifest; it only runs the validation once.
*/ -}}
{{- $_ := include "oriso.domainName" . -}}
